export const MOCK_TRANSCRIPTION_RESPONSE =
  (process.env["MOCK_TRANSCRIPTION_RESPONSE"] || "").toLowerCase() === "true";

export const RESOURCES_PATH = process.env["RESOURCES_PATH"] || "/app/resources";
